name = "measurestack-leadgen"
pages_build_output_dir = "./public"
compatibility_date = "2026-08-05"
compatibility_flags = ["nodejs_compat"]


# Create a D1 database named measurestack-identity, then bind it in the
# Cloudflare Pages dashboard with the variable name MEASURESTACK_DB.
# The database ID is intentionally not committed because it is account-specific.
