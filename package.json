{
  "name": "measurestack-leadgen",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/validate.mjs && node --test",
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=measurestack-leadgen"
  },
  "dependencies": {
    "@clerk/backend": "^3.11.7"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
