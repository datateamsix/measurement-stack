{
  "name": "measurement-stack-leadgen",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/validate.mjs && node --test",
    "build:consent": "npm --prefix consent-sdk run build && node scripts/sync-consent-assets.mjs",
    "dev": "npx wrangler pages dev public",
    "deploy": "npx wrangler pages deploy public --project-name=measurement-stack-leadgen"
  }
}
